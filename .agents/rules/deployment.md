# Deployment Rule

Only execute `git push` or deployment commands when the user explicitly asks to deploy (e.g., "배포해줘", "deploy"). Do not automatically push or deploy after modifying code unless explicitly instructed by the user.
